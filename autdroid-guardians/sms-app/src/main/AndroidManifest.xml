<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.autodroid.sms">

    <!-- 短信相关权限 -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- 默认短信应用所需权限 -->
    <uses-permission android:name="android.permission.BROADCAST_SMS" />
    <uses-permission android:name="android.permission.BROADCAST_WAP_PUSH" />
    <uses-permission android:name="android.permission.SEND_RESPOND_VIA_MESSAGE" />
    
    <!-- 默认短信应用特殊权限 -->
    <uses-permission android:name="android.permission.CHANGE_COMPONENT_ENABLED_STATE" />
    
    <!-- 华为设备特定权限 -->
    <uses-permission android:name="android.permission.WRITE_APN_SETTINGS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- 华为MMS特定权限（如果可用） -->
    <uses-permission android:name="huawei.permmisons.mms.SEND_REQUEST" />
    <uses-permission android:name="huawei.permmisons.mmsandsms.SEND_PERMISSION" />

    <!-- 硬件特性声明 -->
    <uses-feature android:name="android.hardware.telephony" android:required="true" />

    <application
        android:name=".SmsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="短信"
        android:theme="@style/Theme.SmsApp"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        
        <!-- 默认短信应用元数据 -->
        <meta-data
            android:name="android.app.default_sms_app"
            android:value="true" />
            
        <!-- 声明为短信应用 -->
        <meta-data
            android:name="android.app.role.SMS"
            android:value="true" />

        <!-- 主界面 -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- 默认短信应用意图过滤器 -->
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
            </intent-filter>
            
            <!-- 短信发送意图过滤器 -->
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
            
            <!-- 查看短信意图过滤器 -->
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- 短信和彩信接收器 -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- 短信发送状态接收器 -->
        <receiver
            android:name=".receiver.SmsSentReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.autodroid.sms.action.SMS_SENT" />
            </intent-filter>
        </receiver>

        <!-- 短信发送服务 -->
        <service
            android:name=".service.SmsSenderService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.autodroid.sms.action.SEND_SMS" />
                <action android:name="com.autodroid.sms.action.SEND_MMS" />
            </intent-filter>
        </service>
        
        <!-- 短信状态接收器 -->
        <receiver
            android:name=".receiver.SmsStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.autodroid.sms.action.SMS_SENT" />
                <action android:name="com.autodroid.sms.action.SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- 短信响应服务 -->
        <service
            android:name=".service.SmsResponseService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!-- 短信设置界面 -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false" />
            
        <!-- 短信编写界面 -->
        <activity
            android:name=".ui.ComposeActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        
        <!-- 短信会话界面 -->
        <activity
            android:name=".ui.ConversationActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android-dir/mms-sms" />
            </intent-filter>
        </activity>

    </application>

</manifest>
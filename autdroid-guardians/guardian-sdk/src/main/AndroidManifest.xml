<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 无障碍服务权限 -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />

    <!-- 短信相关权限 -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- 默认短信应用所需权限 -->
    <uses-permission android:name="android.permission.BROADCAST_SMS" />
    <uses-permission android:name="android.permission.BROADCAST_WAP_PUSH" />
    <uses-permission android:name="android.permission.SEND_RESPOND_VIA_MESSAGE" />
    
    <!-- 默认短信应用特殊权限 -->
    <uses-permission android:name="android.permission.CHANGE_COMPONENT_ENABLED_STATE" />
    
    <!-- 华为设备特定权限 -->
    <uses-permission android:name="android.permission.WRITE_APN_SETTINGS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- 华为MMS特定权限（如果可用） -->
    <uses-permission android:name="huawei.permmisons.mms.SEND_REQUEST" />
    <uses-permission android:name="huawei.permmisons.mmsandsms.SEND_PERMISSION" />

    <!-- 硬件特性声明 -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- 其他权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- 紧急服务 -->
        <service
            android:name=".service.EmergencyService"
            android:exported="false" />

        <!-- 浮动窗口服务 -->
        <service
            android:name=".service.FloatingWindowService"
            android:exported="false" />

        <!-- 无障碍服务 -->
        <service
            android:name=".service.GuardianAccessibilityService"
            android:label="@string/guardian_accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/guardian_accessibility_service_config" />
        </service>

        <!-- 短信接收器（用于关键词监控） -->
        <receiver
            android:name=".receiver.SmsGuardReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- 音频录音服务 -->
        <service
            android:name=".service.AudioRecordingService"
            android:exported="false"
            android:enabled="true" />

        <!-- 短信发送服务 -->
        <service
            android:name=".sms.service.SmsSenderService"
            android:exported="false" />

        <!-- 短信守卫接收器 -->
        <receiver
            android:name=".sms.receiver.SmsReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_DELIVER_ACTION" />
            </intent-filter>
        </receiver>

        <!-- 短信发送接收器 -->
        <receiver
            android:name=".sms.receiver.SmsSentReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.autodroid.guardiansdk.sms.action.SMS_SENT" />
            </intent-filter>
        </receiver>

        <!-- 短信状态接收器 -->
        <receiver
            android:name=".sms.receiver.SmsStatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.autodroid.guardiansdk.sms.action.SMS_SENT" />
                <action android:name="com.autodroid.guardiansdk.sms.action.SMS_DELIVERED" />
            </intent-filter>
        </receiver>

        <!-- 主界面 -->
        <activity
            android:name=".ui.GuardianActivity"
            android:exported="true"
            android:theme="@style/GuardianAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>

</manifest>
